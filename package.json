{
  "name": "quick-folder-sync",
  "version": "0.0.1",
  "main": "src/index.js",
  "scripts": {
    "build": "rimraf ./dist/* && babel src -d dist"
  },
  "repository": {
    "url": "git@github.com:Klathmon/quick-folder-sync.git",
    "type": "git"
  },
  "author": "Klathmon <gregbenner1@gmail.com>",
  "license": "MIT",
  "babel": {
    "presets": [
      "stage-3",
      [
        "env",
        {
          "targets": {
            "node": 7
          }
        }
      ]
    ]
  },
  "dependencies": {
    "es6-promisify": "^5.0.0",
    "express": "^4.14.0",
    "mkdirp": "^0.5.1",
    "pem": "^1.9.4",
    "request": "^2.79.0",
    "rimraf": "^2.5.4",
    "watchr": "^3.0.1"
  },
  "devDependencies": {
    "babel-cli": "^6.18.0",
    "babel-preset-env": "^1.1.4",
    "babel-preset-stage-3": "^6.17.0"
  }
}
